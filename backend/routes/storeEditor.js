// This file can be used to separate editor routes if desired,
// but we already added them in storeApproval.js grouped under /api/store.
// Keeping for deliverable alignment; it exports the same router from storeApproval for now.
const router = require('./storeApproval');
module.exports = router;


